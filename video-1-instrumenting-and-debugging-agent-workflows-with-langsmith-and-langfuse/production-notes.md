# Production Notes for Video 1

- Use clear, large fonts (16pt+) for code and terminal recordings.
- Zoom into code and dashboard UI elements when explaining key features.
- Narrate commands before executing them on-screen.
- Include both successful and failed runs to illustrate debugging.
- Pause after each major step to allow viewer comprehension.
- Verify SDK versions and update to latest (2024) before recording.
